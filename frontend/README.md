# Frontend

Interfaccia React + Vite dell'applicazione. Per la documentazione completa
del progetto (architettura, setup, guida all'uso con esempi) vedi il
[README principale](../README.md) nella radice del repository.

Avvio rapido:

```bash
npm install
npm run dev
```
